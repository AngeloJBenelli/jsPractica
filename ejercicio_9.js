const numeros = [1, 2, 3, 4, 5]; // Array de ejemplo
let suma = 0;

for (let i = 0; i < numeros.length; i++) {
  suma += numeros[i]; // Sumar cada elemento del array
}

console.log("La suma de los elementos del array es: " + suma);
