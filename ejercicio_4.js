const numero = 10;
if (numero % 2 === 0) { // Verifica si el número es par
  // El operador % devuelve el resto de la división entre el número y 2
    console.log("El número es par.");
  } else {
    console.log("El número no es par.");
  }
